import{_ as e,o as t,c as o,V as d}from"./chunks/framework.4acca30d.js";const m=JSON.parse('{"title":"浏览器中那些容易混淆的宽高属性（MDN）","description":"","frontmatter":{"title":"浏览器中那些容易混淆的宽高属性（MDN）","catalog":true,"date":"2019-05-13T22:35:00.000Z","subtitle":null,"header-img":null,"tags":["JavaScript","HTML","CSS"]},"headers":[],"relativePath":"docs/frontend/html/the-width-in-browser.md","filePath":"docs/frontend/html/the-width-in-browser.md"}'),i={name:"docs/frontend/html/the-width-in-browser.md"},n=d('<h1 id="浏览器中那些容易混淆的宽高属性-mdn" tabindex="-1">浏览器中那些容易混淆的宽高属性（MDN） <a class="header-anchor" href="#浏览器中那些容易混淆的宽高属性-mdn" aria-label="Permalink to &quot;浏览器中那些容易混淆的宽高属性（MDN）&quot;">​</a></h1><h2 id="element-clientwidth" tabindex="-1">Element.clientWidth <a class="header-anchor" href="#element-clientwidth" aria-label="Permalink to &quot;Element.clientWidth&quot;">​</a></h2><p><strong>MDN 定义如下：</strong></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The <code>Element.clientWidth</code> property is zero for inline elements and elements with no CSS; otherwise, it&#39;s the inner width of an element in pixels. It includes padding but excludes borders, margins, and vertical scrollbars (if present).</p><p>This property will round the value to an integer. If you need a fractional value, use <code>element.getBoundingClientRect()</code>.</p></div><p><code>Element.clientWidth</code> 属性表示元素的内部宽度，以像素计。该属性包括内边距，但不包括垂直滚动条（如果有）、边框和外边距。</p><p>该属性值会被四舍五入为一个整数。如果你需要一个小数值，可使用 <code>element.getBoundingClientRect()</code>。</p><p><img src="https://imgs-1251264059.cos.ap-chengdu.myqcloud.com/the-width-in-browser/5c662be2-22b7-4917-90df-e63df7efde56.png" alt="UTOOLS1557742253183.png"></p><p><strong>解释：</strong></p><p><code>Element.clientWidth</code> 属性返回元素节点的 CSS 宽度，同样只对块级元素有效，也是只包括元素本身的宽度和 <code>padding</code>，如果有垂直滚动条，还要减去垂直滚动条的宽度。</p><p><code>document.documentElement</code> 的 <code>clientHeight</code> 属性，返回当前视口的高度（即浏览器窗口的高度），等同于 <code>window.innerHeight</code> 属性减去水平滚动条的高度（如果有的话）。 <code>document.body</code> 的高度则是网页的实际高度。 一般来说，<code>document.body.clientHeight</code> 大于 <code>document.documentElement.clientHeight</code>。</p><h2 id="element-offsetwidth" tabindex="-1">Element.offsetWidth <a class="header-anchor" href="#element-offsetwidth" aria-label="Permalink to &quot;Element.offsetWidth&quot;">​</a></h2><p><strong>MDN 定义如下：</strong></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The <code>HTMLElement.offsetWidth</code> read-only property returns the layout width of an element as an integer.</p><p>Typically, <code>offsetWidth</code> is a measurement in pixels of the element&#39;s CSS width, including any <code>borders</code>, <code>padding</code>, and vertical scrollbars (if rendered). It does not include the width of pseudo-elements such as <code>::before</code> or <code>::after</code>.</p><p>If the element is <code>hidden</code> (for example, by setting <code>style.display</code> on the element or one of its ancestors to &quot;none&quot;), then 0 is returned.</p></div><p><strong>解释：</strong></p><p><code>Element.offsetHeight</code> 属性返回一个整数，表示元素的 CSS 垂直高度（单位像素），包括元素本身的高度、<code>padding</code> 和 <code>border</code>，以及水平滚动条的高度（如果存在滚动条）。</p><p><code>Element.offsetWidth</code> 属性表示元素的 CSS 水平宽度（单位像素），其他都与 <code>Element.offsetHeight</code> 一致。</p><p>这两个属性都是只读属性，只比 <code>Element.clientHeight</code> 和 <code>Element.clientWidth</code> 多了边框的高度或宽度。 如果元素的 CSS 设为不可见（比如 <code>display: none;</code>），则返回 0。</p><h2 id="window-innerwidth" tabindex="-1">window.innerWidth <a class="header-anchor" href="#window-innerwidth" aria-label="Permalink to &quot;window.innerWidth&quot;">​</a></h2><p><strong>MDN 定义如下：</strong></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Width (in pixels) of the browser window viewport including, if rendered, the vertical scrollbar.</p><p>The <code>innerWidth</code> property is supported in any window object like a window, a frame, a frameset, or a secondary window.</p></div><p>浏览器视口（viewport）宽度（单位：像素），如果存在垂直滚动条则包括它。</p><p><code>innerWidth</code> 属性被任一 window 对象支持，如 window、frame、frameset 或 secondary window。</p><p><strong>解释：</strong></p><p>只读属性，声明了窗口的文档显示区的高度和宽度，以像素计。</p><p>这里的宽度和高度不包括菜单栏、工具栏以及滚动条等的高度。</p><p>IE 不支持这些属性。它用 <code>document.documentElement</code> 或 <code>document.body</code> （与 IE 的版本相关）的 <code>clientWidth</code> 和 <code>clientHeight</code> 属性作为替代。</p><h2 id="window-outerwidth" tabindex="-1">window.outerWidth <a class="header-anchor" href="#window-outerwidth" aria-label="Permalink to &quot;window.outerWidth&quot;">​</a></h2><p><strong>MDN 定义如下：</strong></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p><code>Window.outerWidth</code> read-only property returns the width of the outside of the browser window. It represents the width of the whole browser window including sidebar (if expanded), window chrome and window resizing borders/handles.</p><p>To change the size of a window, see <code>window.resizeBy()</code> and <code>window.resizeTo()</code>.</p></div><p><code>Window.outerWidth</code> 获取浏览器窗口外部的宽度。表示整个浏览器窗口的宽度，包括侧边栏（如果存在）、窗口镶边（window chrome）和调正窗口大小的边框（window resizing borders/handles）。</p><p>要改变一个窗口的大小，可参考 <code>window.resizeBy()</code> 和 <code>window.resizeTo()</code>。</p><p>To get the inner width of a window, i.e. the width of the page being displayed, see <code>window.innerWidth</code>.</p><p>要获取一个窗口的内层宽度（inner width），即页面被展示的区域，请参考 <code>window.innerWidth</code>。</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>IE 不支持此属性，且没有提供替代的属性。</p></div><h2 id="window-screen-width" tabindex="-1">window.screen.width <a class="header-anchor" href="#window-screen-width" aria-label="Permalink to &quot;window.screen.width&quot;">​</a></h2><p><strong>MDN 定义如下：</strong></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Note that not all of the width given by this property may be available to the window itself. When other widgets occupy space that cannot be used by the window object, there is a difference in <code>window.screen.width</code> and <code>window.screen.availWidth</code>.</p><p>Internet Explorer will take into account the zoom setting when reporting the screen width. It will only return the real width of the screen if the zoom is set to 100%.</p></div><p>注意，该属性返回的宽度值不一定就是浏览器窗口可使用的宽度。当有其他小工具占据了屏幕空间时，浏览器有时不能占用小工具（如任务栏）占据的空间。<code>window.screen.width</code> 和 <code>window.screen.availWidth</code> 两者不同。</p><p>在返回该值时，IE 会考虑缩放设置。只有在缩放比例为 <code>100%</code> 时，IE 才返回真实的屏幕宽度。</p><p><strong>综上所述：</strong></p><p><code>window.screen.width</code> 属性声明了显示浏览器的屏幕的宽度。</p><h2 id="window-screen-availwidth" tabindex="-1">window.screen.availWidth <a class="header-anchor" href="#window-screen-availwidth" aria-label="Permalink to &quot;window.screen.availWidth&quot;">​</a></h2><p><strong>MDN 定义如下：</strong></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The <code>Screen.availWidth</code> property returns the amount of horizontal space (in pixels) available to the window.</p></div><p>返回浏览器窗口可占用的水平宽度（单位：像素）。</p><p><strong>解释：</strong></p><p><code>window.screen.availWidth</code> 属性声明了显示浏览器的屏幕的可用宽度，以像素计。 在 Windows 这样的操作系统中，这个可用高度不包括分配给半永久特性（如屏幕底部的任务栏）的垂直空间。</p><h2 id="element-getboundingclientrect" tabindex="-1">Element.getBoundingClientRect <a class="header-anchor" href="#element-getboundingclientrect" aria-label="Permalink to &quot;Element.getBoundingClientRect&quot;">​</a></h2><p><strong>MDN 定义如下：</strong></p><p><img src="https://imgs-1251264059.cos.ap-chengdu.myqcloud.com/the-width-in-browser/444c0f67-6388-4f52-837d-f68c296ab317.png" alt="UTOOLS1557743206128.png"></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The <code>Element.getBoundingClientRect()</code> method returns the size of an element and its position relative to the viewport.</p><p>The returned value is a DOMRect object which is the union of the rectangles returned by <code>getClientRects()</code> for the element, i.e., the CSS border-boxes associated with the element. The result is the smallest rectangle which contains the entire element, with read-only left, top, right, bottom, x, y, width, and height properties describing the overall border-box in pixels. Properties other than width and height are relative to the top-left of the viewport.</p><p>Empty border-boxes are completely ignored. If all the element&#39;s border-boxes are empty, then a rectangle is returned with a width and height of zero and where the top and left are the top-left of the border-box for the first CSS box (in content order) for the element.</p><p>The amount of scrolling that has been done of the viewport area (or any other scrollable element) is taken into account when computing the bounding rectangle. This means that the rectangle&#39;s boundary edges (top, right, bottom, left) change their values every time the scrolling position changes (because their values are relative to the viewport and not absolute).</p><p>If you need the bounding rectangle relative to the top-left corner of the document, just add the current scrolling position to the top and left properties (these can be obtained using <code>window.scrollX</code> and <code>window.scrollY</code>) to get a bounding rectangle which is independent from the current scrolling position.</p><p>Scripts requiring high cross-browser compatibility can use <code>window.pageXOffset</code> and <code>window.pageYOffset</code> instead of <code>window.scrollX</code> and <code>window.scrollY</code>.</p></div><p><code>Element.getBoundingClientRect()</code> 方法返回元素的大小及其相对于视口的位置。</p><p>返回值是一个 DOMRect 对象，这个对象是由该元素的 <code>getClientRects()</code> 方法返回的一组矩形的集合, 即：是与该元素相关的 CSS 边框集合 。 DOMRect 对象包含了一组用于描述边框的只读属性——left、top、right 和 bottom，单位为像素。除了 width 和 height 外的属性都是相对于视口的左上角位置而言的。</p><p>空边框盒（译者注：没有内容的边框）会被忽略。如果所有的元素边框都是空边框，那么这个矩形给该元素返回的 width、height 值为 0，left、top 值为第一个 css 盒子（按内容顺序）的 top-left 值。</p><p>当计算边界矩形时，会考虑视口区域（或其他可滚动元素）内的滚动操作，也就是说，当滚动位置发生了改变，top 和 left 属性值就会随之立即发生变化（因此，它们的值是相对于视口的，而不是绝对的）。</p><p>如果你需要获得相对于整个网页左上角定位的属性值，那么只要给 top、left 属性值加上当前的滚动位置（通过 <code>window.scrollX</code> 和 <code>window.scrollY</code>），这样就可以获取与当前的滚动位置无关的值。</p><p>为了跨浏览器兼容，请使用 <code>window.pageXOffset</code> 和 <code>window.pageYOffset</code> 代替 <code>window.scrollX</code> 和 <code>window.scrollY</code>。</p><p><strong>解释：</strong></p><p><code>Element.getBoundingClientRect</code> 方法返回一个对象（<code>DOMRect</code>），提供当前元素节点的大小、位置等信息，基本上就是 CSS 盒状模型的所有信息。</p><ul><li>x：元素左上角相对于视口的横坐标</li><li>y：元素左上角相对于视口的纵坐标</li><li>height：元素高度</li><li>width：元素宽度</li><li>left：元素左上角相对于视口的横坐标，与 x 属性相等</li><li>right：元素右边界相对于视口的横坐标（等于 x + width）</li><li>top：元素顶部相对于视口的纵坐标，与 y 属性相等</li><li>bottom：元素底部相对于视口的纵坐标（等于 y + height）</li></ul><p>由于元素相对于视口（<code>viewport</code>）的位置，会随着页面滚动变化，因此表示位置的四个属性值，都不是固定不变的。 如果想得到绝对位置，可以将 <code>left</code> 属性加上 <code>window.scrollX</code>，<code>top</code> 属性加上<code>window.scrollY</code>。</p><p>注意，<code>getBoundingClientRect</code> 方法的所有属性，都把边框（<code>border</code> 属性）算作元素的一部分。 也就是说，都是从边框外缘的各个点来计算。因此，<code>width</code> 和 <code>height</code> 包括了元素本身 + <code>padding</code> + <code>border</code>。</p><p>另外，上面的这些属性，都是继承自原型的属性，<code>Object.keys</code> 会返回一个空数组，这一点也需要注意。</p>',63),c=[n];function r(l,s,a,h,p,w){return t(),o("div",null,c)}const u=e(i,[["render",r]]);export{m as __pageData,u as default};
